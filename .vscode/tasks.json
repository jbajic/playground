{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "bazel_build",
            "type": "shell",
            "command": "bazelisk build --config=debug //${relativeFileDirname}:${fileBasenameNoExtension}",
            "group": {
                "kind": "build",
                "isDefault": true
            },
        }
    ]
}